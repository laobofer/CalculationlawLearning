# 链表（Linked List）复习文档

---

## 目录

- [1. 链表常见处理方法总结](#1-链表常见处理方法总结)
- [2. 题目详解](#2-题目详解)
  - [2.1 合并两个有序链表](#21-合并两个有序链表)
  - [2.2 合并 K 个有序链表](#22-合并-k-个有序链表)
  - [2.3 反转链表](#23-反转链表)
  - [2.4 反转范围链表](#24-反转范围链表)
  - [2.5 分割链表](#25-分割链表)
  - [2.6 删除单链表的倒数第 k 个节点](#26-删除单链表的倒数第-k-个节点)
  - [2.7 删除排序链表中的重复元素 II](#27-删除排序链表中的重复元素-ii)
  - [2.8 单链表的中间节点](#28-单链表的中间节点)
  - [2.9 环形链表的起始点](#29-环形链表的起始点)
  - [2.10 相交链表](#210-相交链表)
  - [2.11 回文链表](#211-回文链表)
  - [2.12 旋转链表](#212-旋转链表)
  - [2.13 两数相加](#213-两数相加)
  - [2.14 两数相加 II](#214-两数相加-ii)
- [3. 延伸练习](#3-延伸练习)

---

## 1. 链表常见处理方法总结

| 技巧 | 说明 | 典型应用 |
|------|------|----------|
| **虚拟头节点（Dummy Node）** | 创建一个虚拟头节点指向真正的头，避免处理头节点为空的边界情况，最终返回 `dummy.next` | 合并有序链表、删除节点、分割链表 |
| **快慢指针** | 一快一慢两个指针同时遍历，快指针每次走两步，慢指针每次走一步 | 找中点、检测环、找倒数第 k 个 |
| **双指针（同向/相向）** | 两个指针以不同速度或不同起点遍历 | 删除倒数第 N 个节点、相交链表 |
| **递归反转** | 利用递归天然的回溯特性反转链表，思路简洁 | 反转整个链表、反转部分链表 |
| **迭代反转（头插法）** | 遍历链表，将每个节点插入到新链表的头部 | 反转链表、两数相加 II |
| **哈希集合** | 用集合记录节点引用，辅助判断 | 相交链表、检测环 |
| **先成环再断开** | 将链表首尾相连成环，找到新头后断开 | 旋转链表 |

---

## 2. 题目详解

---

### 2.1 合并两个有序链表

- **题目**：[21. 合并两个有序链表](https://leetcode.cn/problems/merge-two-sorted-lists/description/)
- **描述**：将两个升序链表合并为一个新的升序链表并返回。
- **示例**：
  - 输入：`list1 = [1,2,4]`, `list2 = [1,3,4]`
  - 输出：`[1,1,2,3,4,4]`

#### 原始实现

```python
class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        res = ListNode(-1000)
        p = res

        while list1 is not None and list2 is not None:
            if list1.val <= list2.val:
                p.next = list1
                p = p.next
                list1 = list1.next
            else:
                p.next = list2
                p = p.next
                list2 = list2.next

        if list1 is not None:
            p.next = list1
        if list2 is not None:
            p.next = list2

        return res.next
```

#### 标准实现（迭代法 — ⭐推荐）

> **评价**：和你的实现一致，这是最直观、效率最高的写法，面试首选。时间 O(n+m)，空间 O(1)。

```python
class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        dummy = ListNode(-1)
        cur = dummy

        while list1 and list2:
            if list1.val <= list2.val:
                cur.next = list1
                list1 = list1.next
            else:
                cur.next = list2
                list2 = list2.next
            cur = cur.next

        cur.next = list1 or list2  # 简洁写法
        return dummy.next
```

#### 优化版本

```python
class Solution:
    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
        # 递归法 — 代码更简洁，但面试中迭代法更安全（无栈溢出风险）
        if not list1:
            return list2
        if not list2:
            return list1
        if list1.val <= list2.val:
            list1.next = self.mergeTwoLists(list1.next, list2)
            return list1
        else:
            list2.next = self.mergeTwoLists(list1, list2.next)
            return list2
```

---

### 2.2 合并 K 个有序链表

- **题目**：[23. 合并 K 个升序链表](https://leetcode.cn/problems/merge-k-sorted-lists)
- **描述**：合并 k 个升序链表，返回合并后的升序链表。
- **示例**：
  - 输入：`lists = [[1,4,5],[1,3,4],[2,6]]`
  - 输出：`[1,1,2,3,4,4,5,6]`

#### 原始实现

```python
class Solution:
    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:
        import heapq
        if not lists:
            return None

        pq = []
        res = ListNode(-101)
        p = res

        for index, list in enumerate(lists):
            if list is not None:
                heapq.heappush(pq, (list.val, index, list))

        while pq:  # BUG: 这里应该是 while pq，你的代码写成了 while not pq
            _, index, head = heapq.heappop(pq)
            p.next = head
            head = head.next
            if head:
                heapq.heappush(pq, (head.val, index, head))
            p = p.next

        return res.next
```

> ⚠️ **注意**：你的代码中 `while not pq:` 应为 `while pq:`（当堆非空时继续）。这是关键 bug，考试中要注意！

#### 标准实现（最小堆法 — ⭐推荐）

> **评价**：最小堆是合并 K 个有序链表的最优解法。时间 O(n log k)，空间 O(k)。

```python
class Solution:
    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:
        import heapq
        dummy = ListNode(0)
        cur = dummy

        # 将各链表头节点入堆（用 index 避免比较 ListNode）
        heap = []
        for i, head in enumerate(lists):
            if head:
                heapq.heappush(heap, (head.val, i, head))

        while heap:
            val, i, node = heapq.heappop(heap)
            cur.next = node
            cur = cur.next
            if node.next:
                heapq.heappush(heap, (node.next.val, i, node.next))

        return dummy.next
```

#### 优化版本（分治合并）

```python
class Solution:
    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:
        if not lists:
            return None
        # 分治：两两合并
        while len(lists) > 1:
            merged = []
            for i in range(0, len(lists), 2):
                if i + 1 < len(lists):
                    merged.append(self.mergeTwoLists(lists[i], lists[i + 1]))
                else:
                    merged.append(lists[i])
            lists = merged
        return lists[0]

    def mergeTwoLists(self, l1, l2):
        dummy = ListNode(0)
        cur = dummy
        while l1 and l2:
            if l1.val <= l2.val:
                cur.next = l1
                l1 = l1.next
            else:
                cur.next = l2
                l2 = l2.next
            cur = cur.next
        cur.next = l1 or l2
        return dummy.next
```

---

### 2.3 反转链表

- **题目**：[206. 反转链表](https://leetcode.cn/problems/reverse-linked-list)
- **描述**：反转一个单链表。
- **示例**：
  - 输入：`head = [1,2,3,4,5]`
  - 输出：`[5,4,3,2,1]`

#### 原始实现

```python
class Solution:
    # 迭代法
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if head is None or (head is not None and head.next is None):
            return head
        pre, cur, nxt = None, head, head.next
        while cur:
            cur.next = pre
            pre = cur
            cur = nxt
            if nxt is not None:
                nxt = nxt.next
        return pre

    # 递归法
    def reverseList_(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if head is None or (head is not None and head.next is None):
            return head
        new_link_last = head.next
        new_head = self.reverseList_(head.next)
        new_link_last.next = head
        head.next = None
        return new_head
```

#### 标准实现（迭代法 — ⭐推荐）

> **评价**：迭代法简洁高效，面试中更推荐，无栈溢出风险。时间 O(n)，空间 O(1)。

```python
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        pre, cur = None, head
        while cur:
            nxt = cur.next
            cur.next = pre
            pre = cur
            cur = nxt
        return pre
```

#### 优化版本

```python
class Solution:
    def reverseList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        """头插法 — 思路直观：遍历原链表，将每个节点插入新链表头部"""
        new_head = None
        while head:
            nxt = head.next
            head.next = new_head
            new_head = head
            head = nxt
        return new_head
```

---

### 2.4 反转范围链表

- **题目**：[92. 反转链表 II](https://leetcode.cn/problems/reverse-linked-list-ii)
- **描述**：反转链表中从位置 left 到 right 的部分节点。
- **示例**：
  - 输入：`head = [1,2,3,4,5]`, `left = 2`, `right = 4`
  - 输出：`[1,4,3,2,5]`

#### 原始实现

```python
class Solution:
    def reverseBetween(self, head: Optional[ListNode], left: int, right: int) -> Optional[ListNode]:
        if left == 1:
            return self.reverseN(head, right)
        pre = head
        for _ in range(left - 2):
            pre = pre.next
        pre.next = self.reverseN(pre.next, right - left + 1)
        return head

    def reverseN(self, head, n):
        if head is None or head.next is None:
            return head
        pre, cur, nxt = None, head, head.next
        for _ in range(n):
            cur.next = pre
            pre = cur
            cur = nxt
            if nxt is not None:
                nxt = nxt.next
        head.next = cur
        return pre
```

#### 标准实现（⭐推荐）

> **评价**：你的实现采用了"找到 prev → 反转 N 个节点"的模式，思路清晰。下面给出更简洁的「头插法」，只需一次遍历。

```python
class Solution:
    def reverseBetween(self, head: Optional[ListNode], left: int, right: int) -> Optional[ListNode]:
        dummy = ListNode(0, head)
        pre = dummy

        # 走到 left 的前一个节点
        for _ in range(left - 1):
            pre = pre.next

        # 头插法反转 [left, right]
        cur = pre.next
        for _ in range(right - left):
            nxt = cur.next
            cur.next = nxt.next
            nxt.next = pre.next
            pre.next = nxt

        return dummy.next
```

> 头插法思路：每次将 `cur.next` 节点插入到 `pre` 之后，逐步完成反转。比递归法好理解，且只需一次遍历，**考试中强烈推荐**。

---

### 2.5 分割链表

- **题目**：[86. 分隔链表](https://leetcode.cn/problems/partition-list)
- **描述**：将链表按值 x 分割，所有小于 x 的节点排在大于等于 x 的节点之前，保持原有相对顺序。
- **示例**：
  - 输入：`head = [1,4,3,2,5,2]`, `x = 3`
  - 输出：`[1,2,2,4,3,5]`

#### 原始实现

```python
class Solution:
    def partition(self, head: Optional[ListNode], x: int) -> Optional[ListNode]:
        res_left = ListNode(-101)
        res_right = ListNode(-101)
        p = head
        p1 = res_left
        p2 = res_right

        while p is not None:
            if p.val < x:
                p1.next = p
                p1 = p1.next
            else:
                p2.next = p
                p2 = p2.next
            temp = p.next
            p.next = None
            p = temp

        p1.next = res_right.next
        return res_left.next
```

#### 标准实现（⭐推荐）

> **评价**：你的实现是标准解法！维护两条链后拼接，注意断开原 next 防止成环。**强烈推荐考试中使用**。

```python
class Solution:
    def partition(self, head: Optional[ListNode], x: int) -> Optional[ListNode]:
        left_dummy, right_dummy = ListNode(0), ListNode(0)
        left, right = left_dummy, right_dummy

        while head:
            if head.val < x:
                left.next = head
                left = left.next
            else:
                right.next = head
                right = right.next
            head = head.next

        right.next = None       # 防止成环
        left.next = right_dummy.next
        return left_dummy.next
```

---

### 2.6 删除单链表的倒数第 k 个节点

- **题目**：[19. 删除链表的倒数第 N 个结点](https://leetcode.cn/problems/remove-nth-node-from-end-of-list)
- **描述**：删除链表的倒数第 n 个节点，返回头节点。
- **示例**：
  - 输入：`head = [1,2,3,4,5]`, `n = 2`
  - 输出：`[1,2,3,5]`

#### 原始实现

```python
class Solution:
    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:
        list_ = ListNode(-1)
        list_.next = head
        node = self.getNthFromEnd(list_, n + 1)
        node.next = node.next.next
        return list_.next

    def getNthFromEnd(self, head: Optional[ListNode], n: int):
        fast, slow = head, head
        for _ in range(n - 1):
            fast = fast.next
        while fast.next is not None:
            fast = fast.next
            slow = slow.next
        return slow
```

#### 标准实现（⭐推荐）

> **评价**：你的"快慢指针 + dummy 节点"是标准解法。关键是 dummy 节点避免了对删除头节点的特殊处理。

```python
class Solution:
    def removeNthFromEnd(self, head: Optional[ListNode], n: int) -> Optional[ListNode]:
        dummy = ListNode(0, head)
        fast = slow = dummy

        # fast 先走 n 步
        for _ in range(n):
            fast = fast.next

        # 一起走，fast 到尾时 slow 在待删节点前一个
        while fast.next:
            fast = fast.next
            slow = slow.next

        slow.next = slow.next.next
        return dummy.next
```

---

### 2.7 删除排序链表中的重复元素 II

- **题目**：[82. 删除排序链表中的重复元素 II](https://leetcode.cn/problems/remove-duplicates-from-sorted-list-ii)
- **描述**：在一个已排序的链表中删除所有有重复的节点，只保留没有重复出现的元素。
- **示例**：
  - 输入：`head = [1,2,3,3,4,4,5]`
  - 输出：`[1,2,5]`

#### 原始实现

```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        dummyUniq = ListNode(101)
        dummyDup = ListNode(101)
        pUniq, pDup = dummyUniq, dummyDup
        p = head

        while p is not None:
            if (p.next is not None and p.val == p.next.val) or p.val == pDup.val:
                pDup.next = p
                pDup = pDup.next
            else:
                pUniq.next = p
                pUniq = pUniq.next
            p = p.next
            pUniq.next = None
            pDup.next = None

        return dummyUniq.next
```

#### 标准实现（⭐推荐）

> **评价**：你的实现维护了两条链（唯一链和重复链），思路有创意。更常见的做法是「一次遍历 + 跳过所有重复节点」，更简洁。

```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        dummy = ListNode(0, head)
        cur = dummy

        while cur.next and cur.next.next:
            if cur.next.val == cur.next.next.val:
                val = cur.next.val
                while cur.next and cur.next.val == val:
                    cur.next = cur.next.next
            else:
                cur = cur.next

        return dummy.next
```

---

### 2.8 单链表的中间节点

- **题目**：[876. 链表的中间结点](https://leetcode.cn/problems/middle-of-the-linked-list)
- **描述**：返回链表的中间节点。如果有两个中间节点，返回第二个。
- **示例**：
  - 输入：`head = [1,2,3,4,5]`
  - 输出：`[3,4,5]`
  - 输入：`head = [1,2,3,4,5,6]`
  - 输出：`[4,5,6]`

#### 原始实现

```python
class Solution:
    def middleNode(self, head: Optional[ListNode]) -> Optional[ListNode]:
        fast, slow = head, head
        while fast.next is not None and fast.next.next is not None:
            fast = fast.next.next
            slow = slow.next

        return slow if fast.next is None else slow.next
```

#### 标准实现（⭐推荐）

> **评价**：快慢指针，这是标准解法。注意偶数个节点时返回第二个中间节点（即 slow.next）。**思路非常实用**。

更通用的写法：

```python
class Solution:
    def middleNode(self, head: Optional[ListNode]) -> Optional[ListNode]:
        slow = fast = head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
        return slow
```

> 这个版本更简洁：`while fast and fast.next` 停止时，如果有两个中间节点返回的是第二个（即 `slow` 已在第二个中间节点）。

---

### 2.9 环形链表的起始点

- **题目**：[142. 环形链表 II](https://leetcode.cn/problems/linked-list-cycle-ii/)
- **描述**：返回链表开始入环的第一个节点。如果链表无环，返回 null。
- **示例**：
  - 输入：`head = [3,2,0,-4]`, 环在位置 1
  - 输出：返回索引为 1 的节点

#### 原始实现

```python
class Solution:
    def detectCycle(self, head: Optional[ListNode]) -> Optional[ListNode]:
        fast, slow = head, head
        while fast is not None and fast.next is not None:
            fast = fast.next.next
            slow = slow.next
            if fast is slow:
                break

        if fast is None or fast.next is None:
            return None

        slow = head
        while slow is not fast:
            slow = slow.next
            fast = fast.next

        return slow
```

#### 标准实现（⭐推荐）

> **评价**：这是 Floyd 判圈算法，你的实现完全正确。数学原理：相遇后，一个指针从头开始、另一个从相遇点开始，每次各走一步，再次相遇处即为环入口。

```python
class Solution:
    def detectCycle(self, head: Optional[ListNode]) -> Optional[ListNode]:
        slow = fast = head
        # 检测是否有环
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            if slow == fast:
                # 有环，找入口
                slow = head
                while slow != fast:
                    slow = slow.next
                    fast = fast.next
                return slow
        return None
```

---

### 2.10 相交链表

- **题目**：[160. 相交链表](https://leetcode.cn/problems/intersection-of-two-linked-lights)
- **描述**：找到两个单链表相交的起始节点。若不相交则返回 null。
- **示例**：
  - 输入：A = `[4,1,8,4,5]`, B = `[5,0,1,8,4,5]`
  - 输出：相交节点值为 8

#### 原始实现

```python
class Solution:
    def getIntersectionNode(self, headA: ListNode, headB: ListNode) -> Optional[ListNode]:
        set_a = set()
        p = headA
        while p:
            set_a.add(p)
            p = p.next

        p = headB
        while p:
            if p in set_a:
                return p
            p = p.next

        return None
```

#### 标准实现（双指针法 — ⭐推荐）

> **评价**：哈希集合法时间 O(m+n) 空间 O(m)。更优的是双指针法，空间 O(1)。

```python
class Solution:
    def getIntersectionNode(self, headA: ListNode, headB: ListNode) -> Optional[ListNode]:
        p1, p2 = headA, headB
        while p1 != p2:
            p1 = p1.next if p1 else headB
            p2 = p2.next if p2 else headA
        return p1
```

> 原理：p1 走 A→B，p2 走 B→A，两指针走过的路程相同，第一次相遇处即为交点。

---

### 2.11 回文链表

- **题目**：[234. 回文链表](https://leetcode.cn/problems/palindrome-linked-list)
- **描述**：判断链表是否是回文。
- **示例**：
  - 输入：`head = [1,2,2,1]`
  - 输出：`true`
  - 输入：`head = [1,2]`
  - 输出：`false`

#### 原始实现

```python
class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        stack = []
        p1 = head
        p2 = head

        while p1:
            stack.append(p1.val)
            p1 = p1.next

        while stack and p2:
            val = stack.pop()
            if val != p2.val:
                return False
            p2 = p2.next

        return True
```

#### 标准实现（快慢指针 + 反转后半段 — ⭐推荐）

> **评价**：栈法直观但空间 O(n)。最优解是 O(1) 空间的版本：快慢指针找中点 → 反转后半段 → 比较。

```python
class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        # 快慢指针找中点
        slow = fast = head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next

        # 反转后半段
        prev = None
        while slow:
            nxt = slow.next
            slow.next = prev
            prev = slow
            slow = nxt

        # 比较前后两段
        left, right = head, prev
        while right:
            if left.val != right.val:
                return False
            left = left.next
            right = right.next
        return True
```

> **考试建议**：如果对空间没要求（说 O(n) 也可以），用栈法更不容易出错；追求最优空间复杂度时用反转法。

---

### 2.12 旋转链表

- **题目**：[61. 旋转链表](https://leetcode.cn/problems/rotate-list)
- **描述**：将链表每个节点向右移动 k 个位置。
- **示例**：
  - 输入：`head = [1,2,3,4,5]`, `k = 2`
  - 输出：`[4,5,1,2,3]`

#### 原始实现

```python
class Solution:
    def get_end(self, head):
        p = head
        len_ = 1
        while p.next:
            p = p.next
            len_ += 1
        return p, len_

    def rotateRight(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:
        if head is None or head.next is None:
            return head
        ret = head
        p, len_ = self.get_end(head)
        p.next = head
        k = k % len_

        ret_pre = head
        for i in range(len_ - k):
            if i != 0:
                ret_pre = ret_pre.next
            ret = ret.next

        ret_pre.next = None
        return ret
```

#### 标准实现（⭐推荐）

> **评价**：你的"成环再断开"是标准解法。

```python
class Solution:
    def rotateRight(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:
        if not head or not head.next:
            return head

        # 计算长度并成环
        n = 1
        tail = head
        while tail.next:
            tail = tail.next
            n += 1
        tail.next = head

        # 找到新头的前一个节点
        k = k % n
        for _ in range(n - k):
            tail = tail.next

        new_head = tail.next
        tail.next = None
        return new_head
```

---

### 2.13 两数相加

- **题目**：[2. 两数相加](https://leetcode.cn/problems/add-two-numbers)
- **描述**：两个链表分别表示逆序的非负整数，每个节点存储一位数字，求和并以相同形式返回。
- **示例**：
  - 输入：`l1 = [2,4,3]` (342), `l2 = [5,6,4]` (465)
  - 输出：`[7,0,8]` (807)

#### 原始实现

```python
class Solution:
    def get_val(self, head):
        return head.val if head else 0

    def next_node(self, head):
        return head.next if head is not None and head.next else None

    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:
        p1 = l1
        p2 = l2
        ret = ListNode(-1)
        p = ret
        sum_next = 0
        while True:
            sum = self.get_val(p1) + self.get_val(p2) + sum_next
            sum_here = sum % 10
            sum_next = sum // 10
            p.next = ListNode(sum_here)
            p = p.next
            p1, p2 = self.next_node(p1), self.next_node(p2)
            if not p1 and not p2 and sum_next == 0:
                break
        return ret.next
```

#### 标准实现（⭐推荐）

> **评价**：思路正确。下面给出更简洁的写法。

```python
class Solution:
    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:
        dummy = ListNode(0)
        cur = dummy
        carry = 0

        while l1 or l2 or carry:
            v1 = l1.val if l1 else 0
            v2 = l2.val if l2 else 0
            total = v1 + v2 + carry
            carry = total // 10
            cur.next = ListNode(total % 10)
            cur = cur.next
            l1 = l1.next if l1 else None
            l2 = l2.next if l2 else None

        return dummy.next
```

---

### 2.14 两数相加 II

- **题目**：[445. 两数相加 II](https://leetcode.cn/problems/add-two-numbers-ii)
- **描述**：两个链表表示正序的非负整数，求和并以相同形式返回（不允许反转链表）。
- **示例**：
  - 输入：`l1 = [7,2,4,3]` (7243), `l2 = [5,6,4]` (564)
  - 输出：`[7,8,0,7]` (7807)

#### 原始实现

```python
class Solution:
    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:
        stk1 = []
        while l1:
            stk1.append(l1.val)
            l1 = l1.next
        stk2 = []
        while l2:
            stk2.append(l2.val)
            l2 = l2.next

        dummy = ListNode(-1)
        carry = 0
        while stk1 or stk2 or carry > 0:
            val = carry
            if stk1:
                val += stk1.pop()
            if stk2:
                val += stk2.pop()
            carry = val // 10
            val = val % 10
            newNode = ListNode(val)
            newNode.next = dummy.next
            dummy.next = newNode

        return dummy.next
```

#### 标准实现（⭐推荐）

> **评价**：你的"用栈逆序 → 头插法"是标准解法，思路清晰。**考试中推荐使用**。

优化版本（更简洁）：

```python
class Solution:
    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:
        s1, s2 = [], []
        while l1:
            s1.append(l1.val)
            l1 = l1.next
        while l2:
            s2.append(l2.val)
            l2 = l2.next

        head = None
        carry = 0
        while s1 or s2 or carry:
            val = carry
            if s1: val += s1.pop()
            if s2: val += s2.pop()
            carry = val // 10
            node = ListNode(val % 10)
            node.next = head
            head = node

        return head
```

---

## 3. 延伸练习

以下题目与上述题目使用相同的技巧，供自行测试：

| 技巧 | 推荐题目 |
|------|----------|
| **虚拟头节点** | [203. 移除链表元素](https://leetcode.cn/problems/remove-linked-list-elements)、[82. 删除排序链表中的重复元素 II](https://leetcode.cn/problems/remove-duplicates-from-sorted-list-ii) |
| **快慢指针** | [141. 环形链表](https://leetcode.cn/problems/linked-list-cycle)、[287. 寻找重复数](https://leetcode.cn/problems/find-the-duplicate-number)（将数组视为链表） |
| **递归反转** | [206. 反转链表](https://leetcode.cn/problems/reverse-linked-list)、[92. 反转链表 II](https://leetcode.cn/problems/reverse-linked-list-ii) |
| **双指针** | [21. 合并两个有序链表](https://leetcode.cn/problems/merge-two-sorted-lists)、[面试题 02.02. 返回倒数第 k 个节点](https://leetcode.cn/problems/kth-node-from-end-of-list-lcci) |
| **栈/头插法逆序** | [1019. 链表中的下一个更大节点](https://leetcode.cn/problems/next-greater-node-in-linked-list)、[2487. 从链表中移除节点](https://leetcode.cn/problems/remove-nodes-from-linked-list) |
| **综合** | [143. 重排链表](https://leetcode.cn/problems/reorder-list)（找中点 + 反转 + 合并）、[25. K 个一组翻转链表](https://leetcode.cn/problems/reverse-nodes-in-k-group) |

---

> **复习要点**：
> 1. 链表题 90% 可以用 **虚拟头节点 + 双指针** 解决
> 2. 注意 **断链时的顺序**（先保存 next 再断开，防止丢失后续节点）
> 3. 递归写法虽然简洁，但 **迭代法在面试中更安全**（无栈溢出、易于调试）
